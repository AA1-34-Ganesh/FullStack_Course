sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes note and clicks save

    browser->>browser: JavaScript adds note to page

    browser->>server: POST /new_note_spa
    activate server
    server-->>browser: Note saved response
    deactivate server

    Note right of browser: Page does not reload